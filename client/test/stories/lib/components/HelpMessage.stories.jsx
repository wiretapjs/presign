import React from 'react'
import { storiesOf } from '@storybook/react'
import { HelpMessage } from 'lib/components'

storiesOf('HelpMessage', module).add('renders as expected', () => <HelpMessage />)
