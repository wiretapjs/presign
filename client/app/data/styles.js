const tabletSize = 992
const mobileSize = 440

export const maxWidth = {
  tablet: `${tabletSize}px`,
  mobile: `${mobileSize}px`,
}

export const minWidth = {
  tablet: `${mobileSize + 1}px`,
  desktop: `${tabletSize + 1}px`,
}
